<div class='w-full text-center'>
  <h1 class="font-medium leading-tight text-5xl mt-0 mb-6 text-blue-600 content-center" >Bitcoin Blocks Data</h1>
  
  <div class="flex flex-col justify-center" >
    <div class="mb-6 none">
      <%== pagy_nav_js(@pagy) if @pagy.pages > 1%>
    </div>

    <div class="mb-6">
      <%= render 'form' %>
    </div>
      <%= render 'bitcoin_blocks'%>
  </div>
</div>